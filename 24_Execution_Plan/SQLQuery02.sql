-------------------------------------------------------------------------------
-- Display Estimated Execution Plan: Press Ctrl + L or click the button.
-------------------------------------------------------------------------------

-- 1. Activate the 'Display Estimated Execution Plan' button in SSMS (Ctrl + L).
-- 2. Execute the query:

SELECT *
FROM FactInternetSales; 

-- Result: Shows the *predicted* plan without running the query.